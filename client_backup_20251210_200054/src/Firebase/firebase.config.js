/**
 * Firebase config stub.
 * The original project used firebase here. This stub prevents build errors.
 * Replace with your real firebase config and initialization if you need social login.
 */

export const app = {}; // harmless placeholder
export default app;
